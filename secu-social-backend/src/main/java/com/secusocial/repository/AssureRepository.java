package com.secusocial.repository;

import com.secusocial.model.Assure;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AssureRepository extends JpaRepository<Assure, Long> {
}
