package com.secusocial.repository;

import com.secusocial.model.PrescriptionSpecialiste;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PrescriptionSpecialisteRepository extends JpaRepository<PrescriptionSpecialiste, Long> {
}
